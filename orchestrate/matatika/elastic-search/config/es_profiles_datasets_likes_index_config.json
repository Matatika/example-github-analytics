# This file is managed by the 'files-matatika-lab' file bundle and updated automatically when `meltano upgrade` is run.
# To prevent any manual changes from being overwritten, remove the file bundle from `meltano.yml` or disable automatic updates:
#     meltano config --plugin-type=files files-matatika-lab set _update orchestrate/matatika/elastic-search/config/es_profiles_datasets_likes_index_config.json false

{
  "settings": {
    "number_of_shards": 2,
    "number_of_replicas": 1
  },
  "mappings": {
    "dynamic": "true",
    "_source": {
      "enabled": "true"
    },
    "properties": {
      "id": {
        "type": "text"
      },
      "likedDatasets": {
        "type": "text"
      }
    }
  }
}
