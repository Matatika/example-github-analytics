# This file is managed by the 'files-matatika-lab' file bundle and updated automatically when `meltano upgrade` is run.
# To prevent any manual changes from being overwritten, remove the file bundle from `meltano.yml` or disable automatic updates:
#     meltano config --plugin-type=files files-matatika-lab set _update orchestrate/matatika/elastic-logstash/config/logstash.yml false

http.host: "0.0.0.0"
xpack.monitoring.enabled: true
xpack.monitoring.elasticsearch.hosts: [ "${ELASTICSEARCH_HOSTS}" ]
xpack.monitoring.elasticsearch.username: "elastic"
xpack.monitoring.elasticsearch.password: "${MATATIKA_ES_ELASTIC_PASSWORD}"
xpack.monitoring.elasticsearch.ssl.certificate_authority: /etc/certs/ca/ca.crt
pipeline.ecs_compatibility: disabled
